module Data.CaseInsensitive.AsCi
  ( -- unqualified class re-export
  I.FoldCase(I.foldCase)
  , module Data.CaseInsensitive.AsCi
  ) where
-- generated by https://github.com/rvion/ride/tree/master/jetpack-gen

import qualified Data.CaseInsensitive as I


-- ci_map :: forall s1 s2. FoldCase s2 => (s1 -> s2) -> CI s1 -> CI s2
ci_map = I.map

-- ci_mk :: forall s. FoldCase s => s -> CI s
ci_mk = I.mk

type CiCI a = I.CI a
get_ci_original o = I.original o
set_ci_original x o = o { I.original = x}
get_ci_foldedCase o = I.foldedCase o
set_ci_foldedCase x o = o { I.foldedCase = x}
