module Data.CaseInsensitive.AsCi where
-- generated by https://github.com/rvion/ride/tree/master/jetpack-gen

import qualified Data.CaseInsensitive as I

-- ci_map :: forall s1 s2. FoldCase s2 => (s1 -> s2) -> CI s1 -> CI s2
ci_map = I.map

-- ci_mk :: forall s. FoldCase s => s -> CI s
ci_mk = I.mk

type CiCI a = I.CI a
-- ci_foldedCase :: forall s. CI s -> s
ci_foldedCase = I.foldedCase

-- ci_original :: forall s. CI s -> s
ci_original = I.original

-- ci_foldCase :: s -> s
ci_foldCase = I.foldCase

-- ci_foldCaseList :: [s] -> [s]
-- ci_foldCaseList = I.foldCaseList

