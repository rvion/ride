-- generated by rvion/jetpack-gen 
module AsHm where
import Data.HashMap.Strict

-- ($wupdateOrSnocWith) doesn't seem to be exported
-- ($wupdateWith) doesn't seem to be exported

hm_adjust :: forall v k.
  (Eq k, Hashable k) =>
  (v -> v) -> k -> HashMap k v -> HashMap k v
hm_adjust =  T.adjust

hm_fromList :: forall k v. (Eq k, Hashable k) => [(k, v)] -> HashMap k v
hm_fromList =  T.fromList

hm_fromListWith :: forall v k.
  (Eq k, Hashable k) =>
  (v -> v -> v) -> [(k, v)] -> HashMap k v
hm_fromListWith =  T.fromListWith

hm_insert :: forall k v.
  (Eq k, Hashable k) =>
  k -> v -> HashMap k v -> HashMap k v
hm_insert =  T.insert

hm_insertWith :: forall v k.
  (Eq k, Hashable k) =>
  (v -> v -> v) -> k -> v -> HashMap k v -> HashMap k v
hm_insertWith =  T.insertWith

hm_intersectionWith :: forall v1 v2 v3 k.
  (Eq k, Hashable k) =>
  (v1 -> v2 -> v3) -> HashMap k v1 -> HashMap k v2 -> HashMap k v3
hm_intersectionWith =  T.intersectionWith

hm_map :: forall v1 v2 k. (v1 -> v2) -> HashMap k v1 -> HashMap k v2
hm_map =  T.map

hm_mapWithKey :: forall k v1 v2. (k -> v1 -> v2) -> HashMap k v1 -> HashMap k v2
hm_mapWithKey =  T.mapWithKey

hm_singleton :: forall k v. Hashable k => k -> v -> HashMap k v
hm_singleton =  T.singleton

hm_unionWith :: forall v k.
  (Eq k, Hashable k) =>
  (v -> v -> v) -> HashMap k v -> HashMap k v -> HashMap k v
hm_unionWith =  T.unionWith
-- (unsafeInsertWith) doesn't seem to be exported
-- (updateOrSnocWith) doesn't seem to be exported
-- (updateWith) doesn't seem to be exported
